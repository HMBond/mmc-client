export class ViewModel {
  id: number = -1;
  label: string = '';
  backgroundColor: string = 'black';
  place: number = -1; // placement of view and view button
  moduleIds: number[] = []; // list of module ids to render within the view
}
